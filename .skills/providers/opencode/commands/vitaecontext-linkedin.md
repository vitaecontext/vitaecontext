---
description: Audit or improve LinkedIn profile positioning and discoverability
---

Use the `vitaecontext-linkedin` skill before producing output.

Treat `$ARGUMENTS` as the user's LinkedIn profile export, draft section, target role, or optimization instruction. Keep recommendations grounded in the user's supplied facts and clearly separate profile edits from activity strategy.

Use the skill's bounded default scope unless the user asks for full optimization. Include a depth note for broad or incomplete audits, load only task-relevant references, and label unverifiable ranking or algorithm claims as confidence-scored rather than certain.
