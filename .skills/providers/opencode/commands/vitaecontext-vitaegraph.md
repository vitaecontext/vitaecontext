---
description: Create, validate, index, or use a private VitaeGraph
---

Use the `vitaecontext-vitaegraph` skill before producing output.

Treat `$ARGUMENTS` as an optional exact VitaeGraph path, source set, validation request, or retrieval task. Use `~/.vitaecontext/vitaegraph` when no directory is supplied. Do not scan for private data. Inventory supplied material first, finish one relevant domain workflow at a time, keep the compact Career Context file separate, and never publish or overwrite private graph content by default.
