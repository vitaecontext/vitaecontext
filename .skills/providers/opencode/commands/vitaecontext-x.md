---
description: Audit or improve X/Twitter profile and posting strategy
---

Use the `vitaecontext-x` skill before producing output.

Treat `$ARGUMENTS` as the user's profile draft, bio, pinned post, content plan, or optimization instruction. Keep platform claims confidence-scored when they depend on opaque ranking behavior.

Use the skill's bounded default scope unless the user asks for a full account audit. Include a depth note for broad audits, load only task-relevant references, and separate official product capabilities, architectural inference, and empirical tactics.
