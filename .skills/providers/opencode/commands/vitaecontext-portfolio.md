---
description: Audit or improve a personal website or portfolio for SEO and AEO
---

Use the `vitaecontext-portfolio` skill before producing output.

Treat `$ARGUMENTS` as the user's website URL, repository path, page path, or optimization instruction. For code edits, preserve the existing application architecture and styling unless the user explicitly asks for visual redesign.

Use the skill's bounded default audit scope unless the user requests a full site audit. Preserve claim discipline: do not invent facts, metrics, roles, employers, testimonials, clients, or outcomes. Report audit depth and source/context gaps before expanding scope.
